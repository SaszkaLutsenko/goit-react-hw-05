import { InfinitySpin } from 'react-loader-spinner';
const Loader = () => (

  <InfinitySpin
    visible={true}
    width="200"
    color="	#808080"
    ariaLabel="infinity-spin-loading"
    />
  
);

export default Loader;